#include "StreamSet.h"


namespace sensekit {
    StreamSet::StreamSet(StreamSetId id)
    {
    }

    Stream* StreamSet::find_stream(StreamTypeId typeId, int index)
    {
        return nullptr;
    }

    void StreamSet::add_stream(Stream* stream)
    {
    }

    void StreamSet::remove_stream(Stream* stream)
    {
    }
}